# CLAUDE.md

Whenever notes are being created or edited in this repository — writing a new topic folder, editing an existing `NN_*.md` section, or authoring or changing a diagram — **follow the `make-notes` skill** (`.claude/skills/make-notes/SKILL.md`) as the authoritative spec. Load and apply it automatically; the user should never have to invoke `/make-notes` by hand. The skill is the single source of truth for structure, formatting, diagrams, and publishing.

(For what this repository is, see `README.md`.)
